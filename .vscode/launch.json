{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "attach",
      "name": "Launch Chrome against localhost",
      "url": "http://localhost:4200",
      "port": 9222,
      "webRoot": "${workspaceFolder}/client",
      "preLaunchTask": "Install Dependencies - Client"
    }
  ]
}
